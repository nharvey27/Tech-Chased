mutation CreateComment($options: TicketInput!, $ticketId: Int!, $userId: Int!) {
  createComment(options: $options, ticketId: $ticketId, userId: $userId) {
    text
    user {
      username
    }
  }
}
